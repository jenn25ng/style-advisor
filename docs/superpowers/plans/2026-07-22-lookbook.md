# 룩북(Lookbook) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 진단 결과(팔레트+골격)로 플랫레이 룩 카드 3개를 생성해 결과 페이지에 추가한다.

**Architecture:** 기존 구조 준수 — 데이터(`data/lookShapes.ts`), 순수함수 엔진(`engine/lookBuilder.ts`), 표현 컴포넌트(`Result/LookCard.tsx`, `Result/Lookbook.tsx`). 서버·상태 변경 없음(결과 파생 렌더만 추가).

**Tech Stack:** 기존과 동일(Vite+React+TS, Vitest). 인라인 SVG, 외부 이미지 0.

**근거:** `docs/superpowers/specs/2026-07-22-lookbook-design.md`

---

## 타입 추가 (Task L1에서 `src/types.ts`에 append)
```typescript
export type LookItemRole = 'top' | 'bottom' | 'point';
export interface LookItem { role: LookItemRole; name: string; hex: string; }
export interface Look {
  theme: string;      // '데일리' | '포인트 룩' | '모던'
  top: LookItem;
  bottom: LookItem;
  point: LookItem;
  fitLabel: string;   // 골격 기반 핏/실루엣 라벨
}
```

---

## Task L1: 타입 + 룩 데이터 (`lookShapes.ts`)

**Files:**
- Modify: `src/types.ts` (위 Look 타입 append)
- Create: `src/data/lookShapes.ts`
- Test: `src/data/__tests__/lookShapes.test.ts`

- [ ] **Step 1: 타입 append** — 위 블록을 `src/types.ts` 끝에 추가. `npx tsc --noEmit` 통과 확인.

- [ ] **Step 2: 실패 테스트 작성**
```typescript
// src/data/__tests__/lookShapes.test.ts
import { describe, it, expect } from 'vitest';
import { neutralBase, frameSilhouette } from '../lookShapes';

describe('lookShapes', () => {
  it('warm/cool 각각 중립 베이스 색이 2개 이상', () => {
    expect(neutralBase.warm.length).toBeGreaterThan(1);
    expect(neutralBase.cool.length).toBeGreaterThan(1);
    for (const s of [...neutralBase.warm, ...neutralBase.cool]) {
      expect(s.hex).toMatch(/^#[0-9A-Fa-f]{6}$/);
    }
  });
  it('3개 골격 실루엣 파라미터가 모두 정의됨', () => {
    for (const id of ['straight', 'wave', 'natural'] as const) {
      expect(frameSilhouette[id]).toBeTruthy();
      expect(frameSilhouette[id].bottom).toMatch(/pants|skirt|wide/);
    }
  });
});
```

- [ ] **Step 3: 구현** `src/data/lookShapes.ts`
```typescript
import type { FrameTypeId, Swatch } from '../types';

// HEX는 렌더링용 예시(퍼스널컬러 원칙과 동일).
export const neutralBase: Record<'warm' | 'cool', Swatch[]> = {
  warm: [
    { name: '아이보리', hex: '#F3EAD8' },
    { name: '카멜', hex: '#B98C5A' },
    { name: '차콜 브라운', hex: '#4A3F35' },
  ],
  cool: [
    { name: '오프 화이트', hex: '#F2F2F4' },
    { name: '쿨 그레이', hex: '#9AA0A8' },
    { name: '네이비', hex: '#26324A' },
  ],
};

// 골격별 실루엣 파라미터: LookCard가 이 값으로 아이템 모양을 그린다.
export interface Silhouette {
  top: 'boxy' | 'fitted' | 'oversized';
  bottom: 'straight-pants' | 'flare-skirt' | 'wide-pants';
  topWidth: number;   // viewBox 100 기준 상의 폭
  hemWidth: number;   // 하의 밑단 폭(플레어/와이드 표현)
}
export const frameSilhouette: Record<FrameTypeId, Silhouette> = {
  straight: { top: 'boxy',      bottom: 'straight-pants', topWidth: 46, hemWidth: 30 },
  wave:     { top: 'fitted',    bottom: 'flare-skirt',    topWidth: 38, hemWidth: 54 },
  natural:  { top: 'oversized', bottom: 'wide-pants',     topWidth: 58, hemWidth: 48 },
};
```

- [ ] **Step 4: 통과 확인** `npx vitest run src/data/__tests__/lookShapes.test.ts` → PASS.
- [ ] **Step 5: Commit** `feat: 룩 데이터(중립 베이스색 + 골격 실루엣 파라미터) + Look 타입`

---

## Task L2: 룩 생성기 (`lookBuilder.ts`)

**Files:**
- Create: `src/engine/lookBuilder.ts`
- Test: `src/engine/__tests__/lookBuilder.test.ts`

- [ ] **Step 1: 실패 테스트 작성**
```typescript
// src/engine/__tests__/lookBuilder.test.ts
import { describe, it, expect } from 'vitest';
import { buildLooks } from '../lookBuilder';
import { colorTypes } from '../../data/colorTypes';
import { frameTypes } from '../../data/frameTypes';

describe('buildLooks', () => {
  it('모든 24조합에서 룩 3개가 생성되고 각 룩 필드가 채워진다', () => {
    for (const c of colorTypes) for (const f of frameTypes) {
      const looks = buildLooks(c.id, f.id);
      expect(looks).toHaveLength(3);
      for (const lk of looks) {
        expect(lk.theme.length).toBeGreaterThan(0);
        expect(lk.fitLabel.length).toBeGreaterThan(0);
        for (const it of [lk.top, lk.bottom, lk.point]) {
          expect(it.name.length).toBeGreaterThan(0);
          expect(it.hex).toMatch(/^#[0-9A-Fa-f]{6}$/);
        }
      }
    }
  });
  it('한 룩 안에서 상/하/포인트 색은 서로 다르다', () => {
    const looks = buildLooks('spring-bright', 'straight');
    for (const lk of looks) {
      const hexes = new Set([lk.top.hex, lk.bottom.hex, lk.point.hex]);
      expect(hexes.size).toBe(3);
    }
  });
});
```

- [ ] **Step 2: 실패 확인** `npx vitest run src/engine/__tests__/lookBuilder.test.ts`

- [ ] **Step 3: 구현** `src/engine/lookBuilder.ts`
```typescript
import type { ColorTypeId, FrameTypeId, Look, LookItem, Swatch } from '../types';
import { colorTypes } from '../data/colorTypes';
import { buildGuide } from './guideBuilder';
import { neutralBase } from '../data/lookShapes';

const THEMES = ['데일리', '포인트 룩', '모던'];

function toItem(role: LookItem['role'], s: Swatch): LookItem {
  return { role, name: s.name, hex: s.hex };
}

export function buildLooks(colorId: ColorTypeId, frameId: FrameTypeId): Look[] {
  const guide = buildGuide(colorId, frameId);
  const c = colorTypes.find((x) => x.id === colorId)!;
  const neutrals = neutralBase[c.undertone];
  const palette = guide.palette;
  const point = guide.pointColor ?? palette[0];
  const fitLabel = `${guide.silhouettes[0]} · ${guide.fits[0]}`;

  const looks: Look[] = [];
  for (let i = 0; i < 3; i++) {
    const top = palette[i % palette.length];
    // 하의: top과 다른 팔레트색 우선, 없으면 중립
    const bottom =
      palette.find((s) => s.hex !== top.hex) ?? neutrals[i % neutrals.length];
    // 포인트: top/bottom과 다른 색 (point → 팔레트 → 중립 순)
    const usedHex = new Set([top.hex, bottom.hex]);
    const pointPick =
      (!usedHex.has(point.hex) ? point : undefined) ??
      palette.find((s) => !usedHex.has(s.hex)) ??
      neutrals.find((s) => !usedHex.has(s.hex)) ??
      neutrals[(i + 1) % neutrals.length];
    looks.push({
      theme: THEMES[i],
      top: toItem('top', top),
      bottom: toItem('bottom', bottom),
      point: toItem('point', pointPick),
      fitLabel,
    });
  }
  return looks;
}
```
※ 두 번째 테스트(색 3개 상이)를 반드시 통과시켜라. 팔레트가 매우 짧아도 중립 베이스로 보정해 3색이 서로 다르게 만든다. 로직이 부족하면 중립 fallback을 강화하되, "top=팔레트, 나머지는 서로 다른 색"이라는 의도를 유지한다.

- [ ] **Step 4: 통과 확인** `npx vitest run src/engine/__tests__/lookBuilder.test.ts` → PASS.
- [ ] **Step 5: Commit** `feat: 룩 생성기 buildLooks(24조합 3룩, 색 중복 방지)`

---

## Task L3: LookCard + Lookbook 컴포넌트 + 결과 통합

**Files:**
- Create: `src/components/Result/LookCard.tsx`, `src/components/Result/Lookbook.tsx`
- Modify: `src/pages/ResultPage.tsx` (StyleGuideCard 아래 `<Lookbook result={result} />`)
- Modify: `src/index.css` (룩북 스타일 append)
- Test: `src/components/Result/__tests__/LookCard.test.tsx`

- [ ] **Step 1: 실패 테스트 작성**
```tsx
// src/components/Result/__tests__/LookCard.test.tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { LookCard } from '../LookCard';
import type { Look } from '../../types';

const look: Look = {
  theme: '데일리',
  top: { role: 'top', name: '브라이트 코랄', hex: '#FF6F61' },
  bottom: { role: 'bottom', name: '네이비', hex: '#26324A' },
  point: { role: 'point', name: '클리어 골든 옐로', hex: '#F7C948' },
  fitLabel: 'I라인 · 저스트사이즈',
};

describe('LookCard', () => {
  it('테마·핏 라벨과 각 아이템 색 이름을 텍스트로 렌더한다', () => {
    render(<LookCard look={look} frame="straight" />);
    expect(screen.getByText('데일리')).toBeInTheDocument();
    expect(screen.getByText(/브라이트 코랄/)).toBeInTheDocument();
    expect(screen.getByText(/네이비/)).toBeInTheDocument();
    expect(screen.getByText(/클리어 골든 옐로/)).toBeInTheDocument();
    expect(screen.getByText(/I라인/)).toBeInTheDocument();
  });
  it('SVG에 aria-label이 있다(접근성)', () => {
    render(<LookCard look={look} frame="straight" />);
    expect(screen.getByRole('img')).toHaveAttribute('aria-label');
  });
});
```

- [ ] **Step 2: 실패 확인** `npx vitest run src/components/Result/__tests__/LookCard.test.tsx`

- [ ] **Step 3: 구현**
`src/components/Result/LookCard.tsx`:
- props: `{ look: Look; frame: FrameTypeId }`.
- `frameSilhouette[frame]` 파라미터로 인라인 SVG(viewBox="0 0 100 130") 그림: 상의(top 유형별 폭/모양), 하의(straight-pants/flare-skirt/wide-pants를 `hemWidth`로 표현), 포인트(신발/가방 작은 도형). 각 도형 `fill`은 해당 아이템 hex.
- SVG에 `role="img"` + `aria-label={`${look.theme} 룩: 상의 ${look.top.name}, 하의 ${look.bottom.name}, 포인트 ${look.point.name}`}`.
- 아래에 텍스트: 테마(강조), 아이템 3줄 `상의 · {name}` 형식(작은 색점 + 이름), 핏 라벨.
- 도형은 정교한 일러스트가 아니라 **아이콘풍 실루엣**(rect/path 단순 조합). 상의는 몸통 사각형 + 어깨/소매, 하의는 사다리꼴(밑단 폭=hemWidth), 포인트는 작은 원/사각.

`src/components/Result/Lookbook.tsx`:
- props: `{ result: DiagnosisResult }`.
- `const looks = buildLooks(result.colorType, result.frameType);`
- 섹션: `<h3>이렇게 입어보세요</h3>` + 3개 `<LookCard look frame={result.frameType} />`.

`src/pages/ResultPage.tsx`: `StyleGuideCard` 렌더 아래에 `<Lookbook result={result} />` 추가(import 포함).

`src/index.css`: `.lookbook`(제목), `.looks`(모바일 세로 스택 / `@media(min-width:40rem)` 가로 3열), `.look-card`(테두리·라운드·패딩), `.look-figure`(SVG 크기 제한), `.look-items`(색점+이름 줄), `.look-fit`(작은 라벨). 기존 톤(인디고 accent, --border, 라운드) 재사용.

- [ ] **Step 4: 통과 확인** `npx vitest run src/components/Result/__tests__/LookCard.test.tsx` → PASS. 이어서 전체 `npx vitest run`(기존 31 + 신규 통과) 및 `npm run build` 성공 확인.
- [ ] **Step 5: Commit** `feat: 룩북 컴포넌트(LookCard/Lookbook) + 결과 페이지 통합`

---

## Task L4: 브라우저 검증

- [ ] dev 서버(localhost:5173) 새로고침 → 진단 결과 페이지 하단 "이렇게 입어보세요"에 룩 3개 렌더 확인(스크린샷).
- [ ] 콘솔 에러 없음(read_console_messages), 모바일/데스크톱 반응형 확인.
- [ ] 세 골격(스트레이트/웨이브/내추럴)에서 실루엣이 다르게 나오는지 스팟 확인.

## 완료 기준
- 모든 결과에서 룩 3개가 빈 값 없이, 색 이름 병기 + aria-label로 표시.
- 전체 테스트/빌드 통과. 결과 페이지에 자연스럽게 통합. 콘솔 에러 없음.
